https://flask.palletsprojects.com/en/2.3.x/installation/

lancer le venv
`.venv\Scripts\activate`

installer flask
`pip install Flask`
`pip install -U flask-cors`
lancer l'api back

`flask --app hello run`
